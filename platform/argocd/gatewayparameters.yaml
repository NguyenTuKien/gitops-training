# Tham số hạ tầng cho argocd-gw: service NodePort.
apiVersion: gateway.kgateway.dev/v1alpha1
kind: GatewayParameters
metadata:
  name: argocd-gw-params
  namespace: kgateway-system
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  kube:
    service:
      type: NodePort
